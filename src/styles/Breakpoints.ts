export const breakpoints = {
    sm: '576px',
    md: '900px',
    lg: '1440px'
}

export const medias = {
    sm: `max-width: ${breakpoints.lg}`,
    md: `max-width: ${breakpoints.md}`,
    lg: `max-width: ${breakpoints.lg}`
}
