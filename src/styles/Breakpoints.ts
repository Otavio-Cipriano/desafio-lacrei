const breakpoints = {
    sm: '576px',
    md: '900',
    lg: '1440'
}

export const medias = {
    sm: `min-width: ${breakpoints.lg}`,
    md: `min-width: ${breakpoints.md}`,
    lg: `min-width: ${breakpoints.lg}`
}
